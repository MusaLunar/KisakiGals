# 更新说明

## 1.0.0

> 对比版本：0.9.1-beta ｜ **第一个正式版**

从 0.9.1-beta 到 1.0.0 是一次界面重做 + 功能补全。此前的界面是各页面分别堆出来的，
这一版把设计体系、窗口外壳、字体与交互统一起来，并新增「探索」页面、存档目录智能识别、
定时自动备份等能力。

### 🎨 界面全面重做

- **统一的设计体系**：整理出间距 / 圆角 / 阴影 / 动效 / 字号五套设计令牌与一套组件库，
  9 个页面全部重写。此前每页各写各的样式，圆角有 9 种、间距有十几种取值；现在全应用
  只有一套节奏。
- **新的窗口外壳**：侧栏收敛为 6 项（主页 / 游戏库 / 探索 / 统计 / AI 助手 / 设置）；
  顶部标题栏常驻窗口按钮与本局游玩时长；详情 / 编辑 / 添加 / 评分这些全屏页面
  也能正常最小化与关闭（此前全屏页面会盖住窗口按钮）。
- **游戏详情页重排**：封面、作品信息、评分徽标、标签、操作按钮归入同一区域，
  下面按「游玩记录 → 简介与趋势 → 数据来源」分区；标签移到作品信息正下方；
  有背景图时卡片为毛玻璃，并用上浅下深的渐变遮罩保证文字可读。
- **游戏库与探索共用一条筛选栏**：同样的卡片外壳、分组标题与选项样式，两处筛选体验一致。
- **字体统一**：内置思源黑体（Source Han Sans 简体子集），不依赖系统是否装过中文字体；
  修正中文的负字距、过粗字重（合成加粗发虚）与行高偏小的问题，时长与计数使用等宽数字；
  并消除了框架兜底样式造成的「红字 + 黄色下划线」。
- **宽屏与滚动体验**：超宽屏下内容居中限宽，元素不再被拉稀；滚动区常驻滚动条，
  长内容底部有渐隐提示，一眼能看出还能往下滚。
- **评分精确到 0.1**：评分对话框保留五星快速评分，并新增滑块（0.1 步进）与 ±0.1 微调，
  数值显示到一位小数；游戏库列表、统计评分墙与 AI 使用的数据同步为一位小数。

### 🧭 探索（新页面）

- **一页两件事**：浏览 VNDB / Bangumi 榜单（按评分、发售日、评价数、名称排序，可按最低
  评分、年份区间、标签筛选），也能用中文名 / 日文原名 / 罗马音 / 常用简称搜索作品。
- **多来源并行搜索**：关键词会同时查询所有元数据来源并合并去重 —— 别名与简称往往只被
  某一个源收录（例如「金恋」在 VNDB 有中文别名、Bangumi 没有），多源并行之后
  搜「金恋」也能找到《金辉恋曲四重奏》。
- **点开作品即可拿到资源下载链接**：在作品详情里自动用「中文名 → 原名 → 别名」查询各资源站
  的发布页，逐条列出并可「打开下载页」或「一键入库」；每个来源的错误单独展示
  （例如某个站点需要登录），不会因为一个站点失败而整块失败。
- 搜索结果按来源相关度排序，默认包含 R18 作品，封面按你的 NSFW 设置自动打码。

### 💾 存档与备份

- **智能识别存档目录**：综合四个来源给出候选并标注可信度 ——
  ① 游戏**运行期间检测到文件写入**的目录（最可信）
  ② 游戏目录内的 savedata / save / セーブ 等关键词目录
  ③ 用户目录下与作品同名或同厂商的目录（AppData、Local、LocalLow、文档、My Games、Saved Games）
  ④ 注册表 `HKCU\Software` 中记录的路径。
  编辑页点一下即可从候选里选择，也可手动指定。
- **定时自动备份**：新增常驻调度器，按设定间隔自动备份（应用常驻或最小化到托盘期间持续
  生效），也可在退出应用时再备份一次，两者各有最小间隔。自动备份使用
  `kisakigals-auto-` 前缀，**保留策略只清理自动备份，手动备份不会被自动删除**。
- **备份文件更可靠**：同一秒内重复备份改用序号后缀（此前会当作重名处理），并且先写临时
  文件、校验通过后再原子改名，中途意外中断不会留下看似有效的半成品。

### 🤖 AI 助手

- **切换页面后继续生成**：生成任务在后台继续跑，完成后在任意页面弹出通知
  （「AI 总结已生成」「AI 推荐已更新」），回到 AI 页结果仍在。
- **配置更简单**：设置里新增服务商下拉（DeepSeek / OpenAI / 阿里云通义 / 智谱 GLM /
  本地 Ollama / 本地 LM Studio / 自定义），选中后自动填好 Base URL 与推荐模型，
  只需再填 API Key；并新增「最大回复长度」。
- **修复推理模型无响应**：`deepseek-flash`、`deepseek-reasoner` 这类先输出思考过程的模型，
  此前会因为 token 上限太小而返回空内容并报「AI 返回内容为空」；现在会正确取回正文。
- 系统代理失效时自动改用直连；「测试连接」不会再卡在「测试中」。

### 🔧 修复

- 修复安装后启动即报 `no such column` 崩溃（随包 SQLite 关闭了双引号字符串，而迁移语句里
  用了双引号）。
- 修复数据迁移到另一台设备后封面 / 背景丢失（备份现在包含图片，媒体路径改为相对路径）。
- 修复存档备份在同一秒内连续执行时的异常。
- 修复鲲Galgame 元数据字段变化导致的简介为空、R18 判定缺失。
- 修复已失效的系统代理导致搜索与封面下载全部静默失败。
- 修复 AI 通知浮层缺少 Material 祖先、可能崩溃的隐患。
- 修复探索页搜索的若干问题：输入即筛选、关键词结果被本地过滤筛掉、别名搜不到。

### ⚡ 优化

- 主页「最近游玩」改为一排小卡片，一屏可看到多部作品；移除快捷入口区，整体更紧凑。
- 打开游戏详情页不再卡顿：用列表已有数据即时渲染，趋势数据与图片存在性检查加入缓存。
- 跨源整合更准：同一作品的日文名、英文名、罗马音会被识别为同一款游戏。
- 通知、库页刷新、评分徽标、凭据保存等多处状态问题的修复。

---

## 0.9.1-beta

> 对比版本：0.9.0-beta

### 🆕 新功能

- **资源搜索**（侧栏新增独立页面）：聚合鲲Galgame、GAL图书馆、TouchGal、量子ACG、真红小站等资源站的**发布页**，可一键用浏览器打开下载页；也可「入库」直接进入搜刮流程补齐元数据。支持配置 SearchGal 兼容聚合接口（自建部署后一次搜索 27+ 站点）。
- **存档备份**：自动识别存档目录（`savedata` / `save` / `セーブ` 等），支持手动备份与退出游戏后自动备份；恢复前自动生成快照，超出保留份数自动清理最旧的备份。备份为明文目录，可直接翻看与替换。
- **Locale Emulator 转区启动**：设置 → 系统中配置 `LEProc.exe`，再按游戏开启，解决日文原版乱码。
- **设备识别与路径可移植**：记录设备标识与相对路径，换电脑、换盘符或改目录后导入数据库，会自动重新定位游戏目录，不必逐个重设。
- **游戏程序自动识别**：拖入整个游戏文件夹也能找到启动程序（汉化补丁优先 → 体积最大 → 与文件夹同名 → 最新修改）。
- **实时游玩时长**：标题栏常驻显示当前游戏与本局时长，详情页新增「总时长 / 本次游玩 / 平均单次」记录卡片。
- **添加游戏**的封面改由各平台提供（不再使用 VNDB 截图），候选多时可「查看全部」用大图网格挑选。
- **游戏库新增紧凑列表排版**：工具栏一键切换「大封面网格 / 紧凑列表」。紧凑列表为左封面 + 右名称的小卡片，一页可浏览、管理更多游戏（多列自适应，显示开发商、总时长、评分与状态，支持右键菜单与批量选择），选择会记住，下次打开仍生效。

### ⚡ 优化

- 打开游戏详情页不再卡顿：改用列表已有数据即时渲染，趋势数据与图片存在性检查加入缓存。
- 搜索与刮削的网络容错：系统代理失效时自动改用直连，不再出现「搜不到任何游戏」；封面、背景图下载走同一套代理策略，失败会明确提示而不是静默忽略。
- 游戏库封面统一为 2:3；筛选栏「清除全部筛选」移到最上方；编辑信息与评分为全屏页面，编辑区域更大。
- 跨源整合更准：同一作品的日文名、英文名、罗马音会被识别为同一款游戏（例如搜 "shiny sisters" 也能找到日文原名的作品）。
- 大量稳定性修复：通知卡片不再残留在界面上、关窗时进行中的游玩时长不再丢失、平台评分角标正确显示、VNDB/Bangumi 凭据不会被代理设置清空、封面与背景图修改后可正常保存。

### 🐞 修复

- **修复安装后启动即报错**：统计查询里把 `""` 当作字符串字面量，而随包发布的 SQLite 关闭了双引号字符串（DQS=0），会导致「no such column」错误、主页统计无法显示。已改用单引号，并新增「全新数据库 + 随包 SQLite」回归测试防止复发。
- 修复个别情况下搜索不到结果（系统代理失效时自动改为直连）。
- 修复修改封面/背景图后保存无效、通知卡片残留在界面、关闭窗口时本次游玩时长丢失等问题。

### 🔒 安全

- 移除了代理场景下跳过 TLS 证书校验的行为（此前用户 Token 与 AI API Key 存在被中间人劫持的风险）。

---

## 0.9.0-beta

首个测试版：多源元数据刮削（9 源）、游玩时长监控、游戏库与统计、AI 助手、云端同步与评分、备份恢复、插件系统。
